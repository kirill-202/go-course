package main

import (
	// "fmt"
	// "hello"
	// "os"
	//"average"
	//"search_replace"
	//"word_counter"
	"filepackage"
)

func main(){
	// hello := hello.Say(os.Args[1:])
	// fmt.Println(hello)
	// average.Average()
	filepackage.CountFileUnits()
}