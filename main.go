package main

import (
	// "fmt"
	// "hello"
	// "os"
	//"average"
	"search_replace"
)

func main(){
	// hello := hello.Say(os.Args[1:])
	// fmt.Println(hello)
	// average.Average()
	search_replace.SearchReplace()
}